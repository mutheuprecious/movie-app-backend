#   Ensure the posgresql gem is defined in your Gemfile
#   gem 'pg'

default: &default
  adapter: postgresql
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  timeout: 5000

development:
  <<: *default
  url: "postgresql://mutheu:<database_71253080>@localhost:5432/movie app>"

production:
  <<: *default
  url: <%= ENV['DATABASE_URL'] %>